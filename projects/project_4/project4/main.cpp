#include <iostream>
#include <armadillo>
#include <cmath>
//#include <functions.h>
//#include "lib.h"
using namespace std;
//using namespace arma;

int main()
{
    /*double T = 1.0;
    vec anaValues = anaMeanValues(T);
    cout << anaValues << endl;

    int N=2; //num. of spins in each dim.
    mat s(N,N); s.fill(1); //spin matrix
    vec values(5);
    int MCc = 10e3; //#MC-cycles
    double E = -8.0;
    double M = 4.0;
    vec numValues(5);
    for(int cycle=0 ; cycle<MCc ; cycle++){
        Metropolis(N, s, T, E, M);
        numValues = numMeanValues(E, N, numValues);
    }

    vec outputVales = outputValues(T, N,MCc,numValues);

    cout << outputVales << endl;
*/

    return 0;
}











